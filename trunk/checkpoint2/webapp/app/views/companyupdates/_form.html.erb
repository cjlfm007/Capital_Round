<%= form_for(@companyupdate) do |f| %>
  <% if @companyupdate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@companyupdate.errors.count, "error") %> prohibited this companyupdate from being saved:</h2>

      <ul>
      <% @companyupdate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= hidden_field_tag 'startup_id', params[:startup_id] %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :newsdate %><br />
    <%= f.date_select :newsdate %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
